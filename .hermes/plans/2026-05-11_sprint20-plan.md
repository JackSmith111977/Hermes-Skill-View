# SRA-003-20 质量修复 Sprint 3 — 计划

> **目标版本:** SRA v1.2.2
> **估时:** ~5h

**Goal:** daemon.py 职责拆分 + print/logging 统一 + CLI 解耦

**前提:** git checkout feat/v2.0-enforcement-layer && git pull

**验证:** pytest tests/ -q (当前 225 passed)

---

### Task 1: daemon.py 职责拆分 (A9 — ~2h)

**Objective:** 将 daemon.py (937行/5职责) 拆分为 3 个文件

**操作：**
1. 新建 `skill_advisor/runtime/config.py` — 路径常量 + `ensure_sra_home()` + `load_config()` + `save_config()`
2. 新建 `skill_advisor/runtime/commands.py` — 全部 CLI 命令函数 (`cmd_start`~`cmd_install_service`)
3. daemon.py 只保留 `SRaDDaemon` 类
4. 更新 `cli.py` 的 import

**具体步骤：**
```
daemon.py 原结构:
  1-40    import + 常量 + 配置函数        → runtime/config.py
  41-91   ensure_sra_home/load_config    → runtime/config.py
  93-620  SRaDDaemon 类                   ✅ 留原地
  622-937 CLI 命令函数 + systemd 模板     → runtime/commands.py
```

### Task 2: print/logging 统一 (C7 — ~1h)

**Objective:** 消除主力模块中的 print/logging 混用

**操作：**
- `indexer.py` → 2 处 `print()` 改为 `logger.info()`
- `dropin.py` → 已有 logger，检查 print 必要性
- `daemon.py` → 确保所有输出走 `logger` 或 `print` 各自有明确目的
- CLI 模块的 `print()` 保留（面向用户的输出）

### Task 3: CLI↔daemon IPC 解耦 (A3 — ~2h)

**Objective:** CLI 不直接 import daemon 实现，改由 Socket IPC 通信

**操作：**
1. `cli.py` 中的 `cmd_start/stop/status/restart/attach` 改为通过 `_socket_request()` 与 daemon 通信
2. 生命周期命令已存在于 `runtime/commands.py`（Task 1 创建），`cli.py` 导入 commands.py 而非 daemon.py
3. 添加 `DaemonController` 类封装所有 IPC 操作
