---
name: gds-generate-project-context
description: Create optimized project-context.md for AI agent consistency. Use when
  the user says "project context" or "generate context"
category: bmad-method
---
# gds-generate-project-context

Create optimized project-context.md for AI agent consistency. Use when the user says "project context" or "generate context"
